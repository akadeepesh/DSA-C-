#include <iostream> //iostream(I/O stream) is a dictonary which contains input/output commands
using namespace std; 
int main()
{
    cout << "Hello World!"; //cout - console output 
    return 0;
}

//Identifier, Keywords, data types and modifiers
/*
--> starts from (A-Z) or (a-z) or _(underscore) [do not start with number]

--> Keywords can't be used as keywords, these are reserver words. Eg- int, if, for etc..

--> int(4 byte), char(1 byte), bool(1 byte), double(8 byte), float(4 byte), void (0 byte), widechar(2 byte)

*/